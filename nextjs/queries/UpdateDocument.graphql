mutation UpdateDocument($documentID: uuid!, $prompt: String!, $title: String!) {
  update_documents_by_pk(
    pk_columns: { id: $documentID }
    _set: { prompt: $prompt, title: $title }
  ) {
    id
    prompt
    title
  }
}
