query GetPortfolio($portfolioID: uuid!) {
  portfolios_by_pk(id: $portfolioID) {
    id
    name
    members(order_by: { crm_id: asc }) {
      id
      crm {
        id
        name
        avatar
        momentum {
          id
          touches
        }
      }
    }
  }
}
