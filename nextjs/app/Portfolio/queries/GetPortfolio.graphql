query GetPortfolio($portfolioID: uuid!) {
  portfolios_by_pk(id: $portfolioID) {
    id
    name
    members(order_by: { touches: asc }) {
      id
      crm {
        id
        name
        avatar
        momentum {
          id
          touches
        }
      }
    }
  }
}
