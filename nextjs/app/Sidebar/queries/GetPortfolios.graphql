query GetPortfolios($ownerID: uuid!) {
  users_by_pk(id: $ownerID) {
    id
    name
    email
    orgs {
      org {
        id
        name
        portfolios {
          id
          name
          members_aggregate {
            aggregate {
              count
            }
          }
        }
      }
    }
  }
}
