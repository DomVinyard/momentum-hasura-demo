table:
  name: guidance
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - created_at
        - id
        - is_enabled
        - parent_id
        - prompt
        - type
      filter: {}
      allow_aggregations: true
