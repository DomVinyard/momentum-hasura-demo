table:
  name: blocks
  schema: public
object_relationships:
  - name: document
    using:
      manual_configuration:
        column_mapping:
          document_id: id
        insertion_order: null
        remote_table:
          name: documents
          schema: public
