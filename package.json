{
  "name": "momentum-demo",
  "version": "1.0.0",
  "description": "momentum-demo",
  "scripts": {
    "dev": "yarn --cwd ./nextjs install && docker-compose up",
    "dev:reset": "sh docker-reset.sh && docker-compose up --build --remove-orphans --force-recreate",
    "hasura": "cd hasura && hasura console --admin-secret=admin_secret"
  },
  "author": "Me",
  "license": "Commercial",
  "dependencies": {
    "prop-types": "^15.8.1",
    "use-session-storage-state": "^18.1.1"
  },
  "devDependencies": {
    "@types/node": "^18.15.0"
  }
}
